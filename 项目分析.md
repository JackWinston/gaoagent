# GaoAgent 项目分析

## 一、项目定位

GaoAgent 是一个**面向终端的 Python 智能体框架**，用户通过 CLI 与 LLM 交互，LLM 可以调用工具、检索知识库、委托远程 Agent 完成复杂任务。

- **语言**：Python 3.12+
- **CLI 框架**：Click + Rich
- **核心依赖**：`click`、`modelcontextprotocol`（MCP SDK）、`chromadb`、`a2a-sdk`、`uvicorn`、`httpx`、`starlette`、`rich`
- **源码规模**：~11,300 行，30 个文件
- **测试**：243 个用例，全部通过

---

## 二、整体架构（四层）

```
┌─────────────────────────────────────────┐
│  CLI 层 (cli.py, 733行)                 │  Click 命令定义 + Rich 终端输出
├─────────────────────────────────────────┤
│  Router 层 (router.py, 86行)            │  action 字符串 → Route(factory, method)
├─────────────────────────────────────────┤
│  Handlers 层                            │  薄控制层：CoreHandlers, MCPHandlers,
│  (各模块 Handlers.py)                   │  RagHandlers, SkillsHandlers, ApiHandlers,
│                                         │  AgentHandlers
├─────────────────────────────────────────┤
│  Core / Runner 层                       │  业务逻辑核心：Runner 执行引擎、LLM 调用、
│  (core/runner/*.py, ~4000行)            │  工具注册、MCP 客户端、RAG 检索
└─────────────────────────────────────────┘
```

### 设计模式

| 模式 | 用途 |
|------|------|
| Command + Registry | Router 将 30 个 action 映射到 Handler 工厂方法 |
| Strategy | ReAct / PlanAndExecute / Reflection 三种执行策略 |
| Template Method | BaseRunner 定义 `decide()` / `run()` 抽象钩子 |
| Decorator | `@tool_spec()` 为工具函数附加 JSON Schema 元数据 |
| Observer | `StreamCallback` 实时流式输出 LLM 生成内容 |
| Facade | `MCPStdioClientSync` 将异步 MCP SDK 封装为同步接口 |

### 关键文件一览

| 文件 | 行数 | 职责 |
|------|------|------|
| `cli.py` | 733 | CLI 入口，所有子命令定义 |
| `router.py` | 86 | action → Route 分发 |
| `core/runner/ReActRunner.py` | 1035 | 核心执行引擎 |
| `core/runner/Tooling.py` | 1122 | 工具注册与 8 个内置工具 |
| `core/runner/Utils.py` | 1005 | 配置解析、历史管理、LLM 响应归一化 |
| `core/runner/HttpClient.py` | 450 | 零依赖 SSE 流式 HTTP 客户端 |
| `core/runner/FunctionCallProtocol.py` | 296 | LLM 函数调用协议解析 |
| `core/runner/PromptBuilder.py` | 245 | 系统提示词动态构建 |
| `core/runner/PlanAndExecuteRunner.py` | 250 | 任务拆解规划模式 |
| `core/runner/ReflectionRunner.py` | 171 | 自我反思重试模式 |
| `core/runner/BaseRunner.py` | 144 | Runner 抽象基类 |
| `core/runner/Console.py` | 243 | 统一控制台输出（Rich） |
| `core/runner/RunLogger.py` | 167 | 结构化运行日志 |
| `core/CoreConfigDefault.py` | 682 | 配置校验与默认值 |
| `core/CoreInit.py` | 578 | 项目初始化流程 |
| `core/CoreHandlers.py` | 133 | 核心命令处理器 |
| `core/ChatRunner.py` | 270 | 多轮对话 |
| `core/TaskRunner.py` | 131 | 任务模式路由 |
| `rag/RagChromaIndexer.py` | 996 | RAG 索引（ChromaDB + BM25） |
| `rag/RagChromaRetriever.py` | 591 | RAG 混合检索（RRF 融合） |
| `mcp/MCPClientCompat.py` | 461 | MCP 同步兼容层 |
| `mcp/MCPHandlers.py` | 292 | MCP 管理命令 |
| `api/ApiHandlers.py` | 284 | API 配置管理 |
| `skills/SkillsHandlers.py` | 238 | Skills 管理 |
| `agent/AgentHandlers.py` | 182 | A2A 智能体管理 |
| `agent/GaoTaskExecutor.py` | 88 | A2A 服务端任务执行器 |

---

## 三、任务执行流程

以 `gaoagent task "写一个脚本" --mode react` 为例：

```
CLI (click)
  → dispatch("task")
    → CoreHandlers.task()
      → TaskRunner.run()
        → 解析 mode → 实例化 ReActRunner
          → ReActRunner.run():
              1. MCP 工具发现（优先缓存，回退到运行时连接）
              2. PromptBuilder 构建系统提示词
              3. 加载会话历史
              4. 进入步骤循环 (最多 32 步):
                 ┌→ decide() → _callLLM()
                 │    → HttpClient 发送 SSE 流式请求
                 │    → StreamCallback 实时输出推理/内容/工具调用
                 │    → FunctionCallProtocol 解析 LLM 响应
                 │
                 ← StepResult.decision:
                    "final"      → 返回最终结果
                    "tool_calls" → 执行工具 → 写入 observation → 继续循环
                    "thought"    → 写入思考 → 继续循环
                    "retry"      → 重试 LLM 调用
              5. 工具路由: 本地 ToolRegistry → MCP 工具 → 报错
              6. 保存历史，返回 RunResult
```

---

## 四、三种执行模式

| 模式 | 类 | 特点 |
|------|----|------|
| **ReAct** | `ReActRunner` (1035行) | 基础推理-行动循环，每步调用 LLM 决策 |
| **Plan & Execute** | `PlanAndExecuteRunner` (250行) | 先让 LLM 拆解任务为子计划，再逐个用 ReActRunner 执行，支持动态重新规划 |
| **Reflection** | `ReflectionRunner` (171行) | 包装任意 Runner，执行后让 LLM 自我评估是否完成，未完成则附带反馈重试（最多 3 轮） |

模式可组合：`--mode retry,plan` = ReflectionRunner 包装 PlanAndExecuteRunner。

### ReActRunner 核心特性

- **MCP 工具发现**：读取缓存 → 运行时连接 → 缓存写回
- **多模态支持**：CLI `--images` 参数 → base64 编码 → OpenAI 多模态内容格式
- **流式输出**：`StreamCallback` 闭包追踪状态（reasoning / content / tool_call），实时输出到终端
- **工具路由优先级**：本地 ToolRegistry > MCP exported map > 报错
- **历史持久化**：`.gaoagent/history/<session_id>.json`

### PlanAndExecuteRunner 执行流程

1. `_generate_plan()`：调用 LLM 将复杂任务拆解为有序子任务（JSON 数组）
2. 执行循环：每个子任务创建新的 ReActRunner 执行，附带原始目标和已完成任务上下文
3. `_evaluate_and_replan()`：每个子任务完成后调用 LLM 评估是否达成目标，必要时生成新计划
4. 状态持久化：`save_runner_state()` / `load_runner_state()` 支持会话续接

### ReflectionRunner 自我反思

1. 运行 `target_runner` 获取初始结果
2. `_evaluate_result()`：调用 LLM 评估，返回 `is_finished` 和 `feedback`
3. 未完成则将反馈追加到问题中重新运行目标 Runner
4. 最多重试 `max_reflections`（默认 3）次
5. 评估失败时默认"任务完成"，防止死循环

---

## 五、内置工具（8 个）

| 工具 | 功能 | 安全约束 |
|------|------|----------|
| `list_dir` | 目录列表，含文件元信息 | — |
| `read_file` | 读取文本文件 | — |
| `write_file` | 写入文件（自动建目录，支持追加） | — |
| `run_command` | 执行 Shell 命令 | 沙箱限制在项目目录 |
| `search_workspace` | 基于 ripgrep 的全文搜索 | 范围限制在项目根目录内 |
| `ask_user` | 交互式用户输入，支持图片 | — |
| `rag_search` | RAG 知识库语义检索 | — |
| `a2a_call` | 委托远程 A2A Agent 执行 | — |

每个工具通过 `@tool_spec()` 装饰器注册 JSON Schema 元数据，LLM 通过 OpenAI function calling 协议调用。

---

## 六、MCP 集成

### 传输协议

支持 `stdio`、`sse`、`streamable_http` 三种 MCP 传输方式。

### 核心挑战与解决方案

MCP SDK 是纯异步的，Runner 是同步的。`_run_coro()` 函数检测当前事件循环状态：若无运行中的事件循环则直接 `asyncio.run()`；若有则在守护线程中执行协程。

### 工具命名

格式：`mcp__<server>__<tool>`，最长 64 字符，超长时使用 SHA1 截断防冲突。同名工具自动追加 `__2`、`__3` 等后缀。

### 缓存机制

工具 schema 缓存到 `gao_client_mcp_tools_cache.json`，启动时优先读取缓存，避免每次重新连接 MCP 服务器。

### 管理命令

- `mcp list`：列出 MCP 服务配置
- `mcp add`：交互式新增（JSON 对象输入）
- `mcp remove`：删除服务
- `mcp enable / disable`：启用/禁用服务
- `mcp test`：测试连通性，输出工具摘要和耗时

---

## 七、RAG 检索管线

### 架构：双索引混合检索

```
查询 ──┬→ ChromaDB (向量密集检索) ──┐
       │                            ├→ RRF 融合 (k=60) → 排序结果
       └→ BM25 SQLite (稀疏检索) ──┘
```

### 索引阶段（RagChromaIndexer, 996 行）

1. 扫描知识库目录中的 `.md` / `.txt` 文件
2. 文本读取（自动尝试 utf-8、utf-8-sig、gbk 编码）
3. 分块：
   - **默认**：固定窗口滑动（chunk_size=1200, overlap=200）
   - **自定义**：外部 Python 文件提供 `chunk_document()` 函数（通过 `importlib.util` 动态加载）
4. 写入 ChromaDB（批处理 batch_size=64）
5. 构建 BM25 SQLite 索引（词频 + 文档快照）
6. 写入 `index_meta.json`（embedding 模式/模型、分块数、存储路径）

### 双编码模式

- **本地**：`SentenceTransformerEmbeddingFunction`（默认 `all-MiniLM-L6-v2`）
- **远程**：OpenAI 兼容 Embedding API（每个知识库独立配置）

### 增量更新

扫描当前文件生成所有分块 → 查询 ChromaDB 已有 ID → 仅插入新分块（upsert）。BM25 索引缺失或损坏时自动全量重建。

### BM25 SQLite Schema

| 表 | 字段 | 用途 |
|----|------|------|
| `bm25_docs` | doc_id (PK), document, metadata_json, doc_len | 文档存储 |
| `bm25_terms` | (term, doc_id) 复合 PK, tf | 词频索引 |
| `bm25_meta` | key, value | 元数据（版本、知识库名） |

索引 `bm25_terms.term` 用于快速倒排查询。

### BM25 参数

- k1=1.2, b=0.75, k3=1.0
- 分词：正则实现 — 中文字符逐字切分，英文按词切分
- IDF：`log((N - df + 0.5) / (df + 0.5) + 1.0)`
- TF 归一化：`(k1+1)*tf / (k*(1-b+b*dl/avgdl) + tf)`

### 检索阶段（RagChromaRetriever, 591 行）

1. 校验知识库存在且有索引元数据
2. 确保 BM25 索引就绪（缺失则自动重建）
3. **并行执行**（`ThreadPoolExecutor(max_workers=2)`）：
   - 线程 1：`_vector_recall()` — ChromaDB 向量查询
   - 线程 2：`_bm25_recall()` — SQLite BM25 评分
4. **RRF 融合**：
   - 公式：`rrf_score += 1 / (rrf_k + rank)`，rrf_k = 60
   - 两条路径均出现的分块获得累积分数
   - 按 rrf_score 降序排列

### 路径管理

ChromaDB 存储路径：`.gaoagent/rag/.chrome_store/kb_<sha256[:24]>/`

使用哈希前缀避免非 ASCII 路径问题（尤其是 Windows 中文目录名）。BM25 SQLite、index_meta.json 和 ChromaDB 数据均在同一哈希目录下，便于项目与全局配置同步。

---

## 八、A2A（Agent-to-Agent）协议

### 客户端（`a2a_call` 工具）

1. 从 `gao_client_a2a_setting.json` 加载远程 Agent 配置
2. 通过 `a2a.client.A2AClient` 创建任务 `create_task(message=...)`
3. 订阅任务事件 `subscribe_task(task.id)`
4. 处理 `artifact_update`（进度）、`task_complete`、`task_failed` 事件
5. 通过 `asyncio.run()` 桥接异步 SDK 到同步工具执行上下文

### 服务端（`agent serve` 命令）

1. 创建 `AgentCard`（含 skills 定义）
2. 使用 `InMemoryTaskStore` 管理任务状态
3. `GaoTaskExecutor` 桥接 A2A 任务到内部 `ReActRunner`：
   - 从 A2A message parts 提取文本
   - 通过 `asyncio.to_thread()` 在线程池中运行 ReActRunner
   - 以 `Artifact` 对象回传结果
4. 通过 uvicorn 在可配置端口提供服务

---

## 九、Skills 系统

### 结构

文件型知识模块，每个 Skill 是包含 `SKILL.md` 的目录：

```markdown
---
name: my-skill
description: Does something useful
---
（LLM 在需要时读取的 Markdown 内容）
```

### 作用域

- **全局仓库**：`~/.gaoagent/skills/<skill_name>/SKILL.md`
- **项目作用域**：`<project>/.gaoagent/skills/<skill_name>/SKILL.md`

### 生命周期

- `gaoagent skills add`：从全局仓库复制到项目作用域
- `gaoagent skills remove`：从项目作用域删除
- `gaoagent skills list`：列出当前作用域可见的 Skills

### 运行时注入

`PromptBuilder` 扫描 Skill 元数据（名称 + 描述），将索引写入系统提示词的 "Skill index" 部分，指示 LLM 仅在相关时读取 `SKILL.md` 全文。

### 元数据解析

`parse_skill_frontmatter_with_reason()` 在 `Utils.py` 中实现：
- 自实现 YAML frontmatter 解析器（无 PyYAML 依赖）
- 支持块标量指示符（`>`、`|`、`|-`）
- 返回 `(metadata, error_reason)` 元组，解析失败时回退到目录名 + 空描述

---

## 十、配置管理

### 双作用域模型

- **全局**：`~/.gaoagent/` — 所有 API、MCP、Skills、RAG 的"仓库"
- **项目**：`<project_root>/.gaoagent/` — 初始化时从全局精选的子集
- 解析逻辑：`try_project_root_dir()` 先检查 CWD 的 `.gaoagent/`，再回退到项目注册表 `inited_projects.txt`

### 配置文件清单

| 文件 | 位置 | 用途 |
|------|------|------|
| `gao_client_api_config.json` | 全局/项目 | API 端点、密钥、模型定义 |
| `gao_client_mcp_setting.json` | 全局/项目 | MCP 服务器配置 |
| `gao_client_mcp_tools_cache.json` | 项目 | MCP 工具 schema 缓存 |
| `gao_client_a2a_setting.json` | 全局/项目 | 远程 A2A Agent 配置 |
| `gao_client_rag_api_config.json` | 每个知识库 | RAG Embedding API 配置 |
| `inited_projects.txt` | 全局 | 已初始化项目路径注册表 |
| `env.json` | 全局 | Debug 模式开关 |
| `history/*.json` | 项目 | 会话历史与 Runner 状态持久化 |
| `logs/*.log` | 项目 | 结构化运行日志 (JSONL) |

### API 配置结构

```json
{
  "default_api": "openai",
  "default_model": "gpt-4.1",
  "apis": {
    "openai": {
      "base_url": "https://api.openai.com/v1",
      "api_key": "sk-...",
      "models": {
        "gpt-4.1": {
          "id": "gpt-4.1",
          "context_window": 128000,
          "capabilities": {"vision": true, "tools": true, "reasoning": false},
          "aliases": []
        }
      }
    }
  }
}
```

### 作用域解析

- `try_project_root_dir()` 返回有效路径 → "project" 作用域
- 否则 → "global" 作用域
- 项目作用域下 `api add` 会同步写入全局配置并设为默认

### 原子写入

所有配置文件写入使用 `tmp_file.replace(target)` 模式，防止中断导致文件损坏。

---

## 十一、LLM 通信协议

### HTTP 层（HttpClient, 450 行）

- 基于 `urllib.request`，零第三方 HTTP 依赖
- 所有请求均为 SSE 流式（`"stream": true`）
- `StreamCallback` 回调 4 种 chunk 类型：`reasoning`、`content`、`tool_call_start`、`tool_call_args`
- 从流式积分块合成标准 `chat.completion` JSON 响应

### 协议解析（FunctionCallProtocol, 296 行）

`map_chat_completion_to_protocol()` 将 LLM 原始响应归一化为内部协议对象：

| 响应类型 | 处理方式 |
|----------|----------|
| `tool_calls` | 原生函数调用 → `StepResult(decision="tool_calls")` |
| `{"type": "final", "content": "..."}` | 最终答案 → `StepResult(decision="final")` |
| `{"type": "thought", "content": "..."}` | 中间思考 → `StepResult(decision="thought")` |
| `{"type": "retry", "content": "..."}` | 重试请求 → `StepResult(decision="retry")` |
| 纯文本 | 视为最终答案 |
| 连续 JSON 对象序列 | 取最后一个有类型标记的对象 |
| 代码围栏包裹 | 自动剥离 ` ```json...``` ` |

### 提示词构建（PromptBuilder, 245 行）

根据模式动态构建系统提示词：
- **ReAct 模式**：输出协议规则 + 工具列表 + RAG 知识库名 + A2A Agent 列表 + Skill 索引 + 系统信息（OS、Python 版本、时间、项目目录）
- **Plan 模式**：指示模型作为"任务规划与评估专家"，要求严格 JSON 输出
- **Reflection 模式**：评估任务完成度的结构化 JSON 反馈

ReAct 提示词中的关键协议规则：
1. 优先使用自身知识，仅在必要时调用工具
2. 每轮对话最多 1 次工具调用
3. 收到工具结果后必须先输出 `thought` JSON 再决定下一步
4. 禁止伪造工具结果

---

## 十二、技术亮点

1. **零第三方 HTTP 客户端**：LLM 调用基于 `urllib.request`，避免依赖冲突
2. **流式优先**：所有 LLM 请求均为 SSE 流式，客户端累积分块后合成标准响应
3. **自实现 BM25 + RRF**：用 SQLite 持久化，正则分词，无 rank_bm25 等外部依赖
4. **多模态贯穿全链路**：从 CLI `--images` 参数到 `ask_user` 工具到 LLM 视觉内容
5. **ContextVar 日志传播**：线程安全的 per-request 日志器，无需层层传递参数
6. **优雅降级**：MCP 不可用时显式失败；BM25 缺失时自动重建；Reflection 评估失败时默认完成防死循环
7. **原子写入保障**：所有配置写入使用 `tmp_file.replace(target)`，防止中断损坏
8. **同步-异步桥接**：`_run_coro()` 智能检测事件循环状态，守护线程运行异步 MCP 协程
